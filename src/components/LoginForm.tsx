import styles from './LoginForm.module.css';

const LoginForm = () => {
  return (
    <div className={styles.placeholder}>
      <h1 className={styles.heading}>Login Form</h1>
      <p className={styles.subtext}>Full form UI coming in Epic 4</p>
    </div>
  );
};

export default LoginForm;
